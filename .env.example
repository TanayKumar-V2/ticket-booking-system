# Core API
PORT=3000
NODE_ENV=development

# Database (Neon Serverless Postgres)
# We use Neon branches for environments (e.g. main for prod, dev for local/preview)
DATABASE_URL=postgresql://neondb_owner:npg_gZrNGi9ROoE3@ep-cold-poetry-atzdsn5f-pooler.c-9.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
# Auth / JWT
JWT_ACCESS_SECRET=super_secret_access_key
JWT_REFRESH_SECRET=super_secret_refresh_key
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# Redis (BullMQ / Rate Limiting)
REDIS_URL=redis://localhost:6379

# Frontend URLs for CORS
FRONTEND_URL=http://localhost:3001
